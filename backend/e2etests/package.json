
{
  "name": "e2etests",
  "version": "1.0.0",
  "description": "api testing",
  "main": "index.js",
  "_comment": "the *.post.test are run after normal types of test to avoid breaking them",
  "scripts": {
    "test": "./node_modules/.bin/mocha *.test.js *.test.after.js --exit --timeout 10000"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "axios": "^0.21.4",
    "chai": "^4.3.4",
    "mocha": "^9.1.1",
    "supertest": "^6.1.6"
  },
  "type": "module",
  "dependencies": {
    "dotenv": "^10.0.0"
  }
}
